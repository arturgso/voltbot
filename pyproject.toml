[project]
name = "enel-auto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Artur Oliveira", email = "arturgs.dev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
enel-auto = "enel_auto:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
