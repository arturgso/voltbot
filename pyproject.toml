[project]
name = "enel-auto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Artur Oliveira", email = "arturgs.dev@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "imap-tools>=1.15.0",
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "pyyaml>=6.0.3",
    "pytest>=8.0.0",
]

[project.scripts]
enel-auto = "enel_auto.main:main"
enel-auto-send-test = "enel_auto.manual_send:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
